server:
  port: 10011

kafka:
  stream:
    application:
      id: fund-service-kafka-stream
  broker:
    host: 192.168.56.101:9092
  zookeeper:
    host: 192.168.56.101:2181

spring:
  profiles: default
  datasource:
    url: jdbc:h2:mem:inventorydb
    username: sa
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
---

server:
  port: 10011

spring:
  profiles: docker
  datasource:
    driver-class-name: com.mysql.jdbc.Driver 
    url: jdbc:mysql://mariadb:3306/inventorydb
    username: root
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
